- move to archive should be action
- retrieve chart call should also return events (if expand parameter is provided?)
- remove list events for chart call (also in PHP client)
- find a way to auto publish successful builds
- php: book and hold calls must be able to take bestAvailable
- docs
- readme